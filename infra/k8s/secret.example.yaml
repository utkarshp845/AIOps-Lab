apiVersion: v1
kind: Secret
metadata:
  name: ai-sre-assistant-secrets
  namespace: ai-infra-starter-kit
  labels:
    app.kubernetes.io/name: ai-sre-assistant-secrets
    app.kubernetes.io/part-of: ai-infra-starter-kit
type: Opaque
stringData:
  # Optional. Leave empty for the default rule-based assistant.
  # Do not commit real API keys.
  OPENAI_API_KEY: ""

