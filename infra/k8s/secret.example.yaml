apiVersion: v1
kind: Secret
metadata:
  name: ai-sre-assistant-secrets
  namespace: aiops-lab
  labels:
    app.kubernetes.io/name: ai-sre-assistant-secrets
    app.kubernetes.io/part-of: aiops-lab
type: Opaque
stringData:
  # Example only. Leave empty for the default rule-based assistant.
  # For a real local key, create infra/k8s/secret.local.yaml instead.
  # Do not commit real API keys.
  OPENAI_API_KEY: ""
